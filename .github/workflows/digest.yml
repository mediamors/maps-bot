name: Maps Bot
on:
  workflow_dispatch:
  schedule:
    # 15:20 МСК = 12:20 UTC
    - cron: '20 12 * * 1'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    - run: pip install feedparser requests
    
    - name: Send Header
      if: github.event.schedule == '20 12 * * 1' || github.event_name == 'workflow_dispatch'
      run: python bot.py HEADER
      
    - name: Marketing
      if: github.event.schedule == '20 12 * * 1' || github.event_name == 'workflow_dispatch'
      run: python bot.py MARKETING
      
    - name: Russia
      if: github.event.schedule == '20 12 * * 1' || github.event_name == 'workflow_dispatch'
      run: python bot.py RU
      
    - name: World
      if: github.event.schedule == '20 12 * * 1' || github.event_name == 'workflow_dispatch'
      run: python bot.py WORLD
